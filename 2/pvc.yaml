kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: sample-database
  namespace: fiap
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: standard